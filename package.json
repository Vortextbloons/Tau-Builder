{
  "dependencies": {
    "@minecraft/server": "^2.10.0",
    "@minecraft/server-ui": "^2.2.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.12",
    "typescript": "^5.9.2"
  },
  "name": "tau builder",
  "version": "1.0.1",
  "private": true,
  "scripts": {
    "build": "node node_modules/esbuild/bin/esbuild src/main.ts --bundle --format=esm --target=es2020 --outfile=Tau_Builder_BP/scripts/main.js --external:@minecraft/server --external:@minecraft/server-ui && node scripts/deploy.js dev",
    "deploy:dev": "node scripts/deploy.js dev",
    "deploy:prod": "node scripts/deploy.js prod",
    "typecheck": "tsc --noEmit"
  }
}
